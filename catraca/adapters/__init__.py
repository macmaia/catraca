"""Adapters that plug the gate into other stacks.

This release ships two: ``catraca.adapters.python`` (a decorator for plain
functions, sync or async) and ``catraca.adapters.mcp`` (middleware for MCP
servers). Neither is imported by the core.
"""
